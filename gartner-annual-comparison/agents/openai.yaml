interface:
  display_name: "Gartner 年度对比"
  short_description: "对比同一 Gartner Hype Cycle 的年度版本"
  default_prompt: "Use $gartner-annual-comparison only as a compatibility entry, then continue with $gartner-hype-cycle-analysis mode=annual-hc."

policy:
  allow_implicit_invocation: false
