interface:
  display_name: "Gartner HC×MQ 洞察"
  short_description: "交叉解释 Gartner HC 与 MQ 信号"
  default_prompt: "Use $gartner-hc-mq-insight to combine completed HC and MQ analyses into evidence-backed cross-report insight, optionally with a target vendor or domain profile."

policy:
  allow_implicit_invocation: false
